---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: coffeeshopservices.platform.t2dzung.github.io
spec:
  group: platform.t2dzung.github.io
  names:
    kind: CoffeeShopService
    listKind: CoffeeShopServiceList
    plural: coffeeshopservices
    singular: coffeeshopservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.desiredReplicas
      name: Desired
      type: integer
    - jsonPath: .status.readyReplicas
      name: Available
      type: integer
    - jsonPath: .spec.image.repository
      name: Image
      type: string
    - jsonPath: .spec.managementPolicy
      name: Management
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CoffeeShopService is the Schema for the coffeeshopservices API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CoffeeShopServiceSpec defines the stateless workload contract.
            properties:
              adoptionPolicy:
                default: Never
                description: AdoptionPolicy controls whether a same-name unowned resource
                  may be adopted.
                enum:
                - Never
                - Explicit
                type: string
              availability:
                description: AvailabilitySpec groups Phase 6.3 availability guardrails.
                properties:
                  pdb:
                    description: PDBSpec intentionally excludes percentages so CEL
                      can compare it to replicas.
                    properties:
                      enabled:
                        type: boolean
                      minAvailable:
                        format: int32
                        minimum: 1
                        type: integer
                    required:
                    - enabled
                    type: object
                  topologySpread:
                    description: TopologySpreadSpec configures fixed hostname spreading
                      for v0.1.
                    properties:
                      enabled:
                        type: boolean
                      whenUnsatisfiable:
                        default: ScheduleAnyway
                        enum:
                        - DoNotSchedule
                        - ScheduleAnyway
                        type: string
                    required:
                    - enabled
                    type: object
                type: object
              env:
                items:
                  description: EnvVarSpec declares a literal or referenced environment
                    variable.
                  properties:
                    name:
                      maxLength: 253
                      pattern: ^[A-Za-z_][A-Za-z0-9_]*$
                      type: string
                    value:
                      maxLength: 4096
                      type: string
                    valueFrom:
                      description: EnvVarSourceSpec is a union of supported external
                        value sources.
                      properties:
                        configMapKeyRef:
                          description: ConfigMapKeySelectorSpec selects one ConfigMap
                            key.
                          properties:
                            key:
                              maxLength: 253
                              minLength: 1
                              type: string
                            name:
                              maxLength: 253
                              minLength: 1
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        secretKeyRef:
                          description: SecretKeySelectorSpec selects one Secret key
                            without reading its data.
                          properties:
                            key:
                              maxLength: 253
                              minLength: 1
                              type: string
                            name:
                              maxLength: 253
                              minLength: 1
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of configMapKeyRef or secretKeyRef must
                          be specified
                        rule: has(self.configMapKeyRef) != has(self.secretKeyRef)
                  required:
                  - name
                  type: object
                  x-kubernetes-validations:
                  - message: exactly one of value or valueFrom must be specified
                    rule: has(self.value) != has(self.valueFrom)
                maxItems: 64
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              image:
                description: ImageSpec identifies exactly one tag-addressed or digest-addressed
                  image.
                properties:
                  digest:
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  pullPolicy:
                    default: IfNotPresent
                    description: PullPolicy describes a policy for if/when to pull
                      a container image
                    enum:
                    - Always
                    - IfNotPresent
                    - Never
                    type: string
                  repository:
                    maxLength: 512
                    minLength: 1
                    type: string
                  tag:
                    pattern: ^[A-Za-z0-9_][A-Za-z0-9_.-]{0,127}$
                    type: string
                required:
                - repository
                type: object
                x-kubernetes-validations:
                - message: exactly one of tag or digest must be specified
                  rule: has(self.tag) != has(self.digest)
              managementPolicy:
                description: ManagementPolicy controls whether the future reconciler
                  may mutate children.
                enum:
                - Observe
                - Manage
                type: string
              networkPolicy:
                description: NetworkPolicySpec configures Phase 6.4 dependency policies.
                properties:
                  egress:
                    items:
                      description: EgressRuleSpec declares one same-namespace stateless
                        dependency.
                      properties:
                        port:
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        protocol:
                          default: TCP
                          description: Protocol defines network protocols supported
                            for things like container ports.
                          enum:
                          - TCP
                          - UDP
                          - SCTP
                          type: string
                        service:
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                      required:
                      - port
                      - service
                      type: object
                    maxItems: 64
                    type: array
                    x-kubernetes-list-map-keys:
                    - service
                    - port
                    x-kubernetes-list-type: map
                  enabled:
                    type: boolean
                required:
                - enabled
                type: object
              ports:
                items:
                  description: ContainerPortSpec declares a named container port.
                  properties:
                    containerPort:
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    name:
                      maxLength: 15
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    protocol:
                      default: TCP
                      description: Protocol defines network protocols supported for
                        things like container ports.
                      enum:
                      - TCP
                      - UDP
                      - SCTP
                      type: string
                  required:
                  - containerPort
                  - name
                  type: object
                maxItems: 32
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              probes:
                description: ProbesSpec configures supported lifecycle probes.
                properties:
                  liveness:
                    description: ProbeSpec excludes exec, host and arbitrary headers
                      and accepts one handler.
                    properties:
                      failureThreshold:
                        default: 3
                        format: int32
                        maximum: 60
                        minimum: 1
                        type: integer
                      grpc:
                        description: GRPCAction configures a Kubernetes gRPC health
                          probe.
                        properties:
                          port:
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          service:
                            maxLength: 63
                            type: string
                        required:
                        - port
                        type: object
                      httpGet:
                        description: HTTPGetAction is the safe HTTP probe subset supported
                          by v0.1.
                        properties:
                          path:
                            default: /
                            pattern: ^/.*
                            type: string
                          port:
                            maxLength: 15
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                            type: string
                          scheme:
                            default: HTTP
                            description: URIScheme identifies the scheme used for
                              connection to a host for Get actions
                            enum:
                            - HTTP
                            - HTTPS
                            type: string
                        required:
                        - port
                        type: object
                      initialDelaySeconds:
                        default: 0
                        format: int32
                        maximum: 300
                        minimum: 0
                        type: integer
                      periodSeconds:
                        default: 10
                        format: int32
                        maximum: 300
                        minimum: 1
                        type: integer
                      successThreshold:
                        default: 1
                        format: int32
                        maximum: 10
                        minimum: 1
                        type: integer
                      tcpSocket:
                        description: TCPSocketAction is the safe TCP probe subset
                          supported by v0.1.
                        properties:
                          port:
                            maxLength: 15
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                            type: string
                        required:
                        - port
                        type: object
                      timeoutSeconds:
                        default: 1
                        format: int32
                        maximum: 60
                        minimum: 1
                        type: integer
                    type: object
                    x-kubernetes-validations:
                    - message: exactly one of httpGet, tcpSocket or grpc must be specified
                      rule: '(has(self.httpGet) ? 1 : 0) + (has(self.tcpSocket) ?
                        1 : 0) + (has(self.grpc) ? 1 : 0) == 1'
                  readiness:
                    description: ProbeSpec excludes exec, host and arbitrary headers
                      and accepts one handler.
                    properties:
                      failureThreshold:
                        default: 3
                        format: int32
                        maximum: 60
                        minimum: 1
                        type: integer
                      grpc:
                        description: GRPCAction configures a Kubernetes gRPC health
                          probe.
                        properties:
                          port:
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          service:
                            maxLength: 63
                            type: string
                        required:
                        - port
                        type: object
                      httpGet:
                        description: HTTPGetAction is the safe HTTP probe subset supported
                          by v0.1.
                        properties:
                          path:
                            default: /
                            pattern: ^/.*
                            type: string
                          port:
                            maxLength: 15
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                            type: string
                          scheme:
                            default: HTTP
                            description: URIScheme identifies the scheme used for
                              connection to a host for Get actions
                            enum:
                            - HTTP
                            - HTTPS
                            type: string
                        required:
                        - port
                        type: object
                      initialDelaySeconds:
                        default: 0
                        format: int32
                        maximum: 300
                        minimum: 0
                        type: integer
                      periodSeconds:
                        default: 10
                        format: int32
                        maximum: 300
                        minimum: 1
                        type: integer
                      successThreshold:
                        default: 1
                        format: int32
                        maximum: 10
                        minimum: 1
                        type: integer
                      tcpSocket:
                        description: TCPSocketAction is the safe TCP probe subset
                          supported by v0.1.
                        properties:
                          port:
                            maxLength: 15
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                            type: string
                        required:
                        - port
                        type: object
                      timeoutSeconds:
                        default: 1
                        format: int32
                        maximum: 60
                        minimum: 1
                        type: integer
                    type: object
                    x-kubernetes-validations:
                    - message: exactly one of httpGet, tcpSocket or grpc must be specified
                      rule: '(has(self.httpGet) ? 1 : 0) + (has(self.tcpSocket) ?
                        1 : 0) + (has(self.grpc) ? 1 : 0) == 1'
                  startup:
                    description: ProbeSpec excludes exec, host and arbitrary headers
                      and accepts one handler.
                    properties:
                      failureThreshold:
                        default: 3
                        format: int32
                        maximum: 60
                        minimum: 1
                        type: integer
                      grpc:
                        description: GRPCAction configures a Kubernetes gRPC health
                          probe.
                        properties:
                          port:
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          service:
                            maxLength: 63
                            type: string
                        required:
                        - port
                        type: object
                      httpGet:
                        description: HTTPGetAction is the safe HTTP probe subset supported
                          by v0.1.
                        properties:
                          path:
                            default: /
                            pattern: ^/.*
                            type: string
                          port:
                            maxLength: 15
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                            type: string
                          scheme:
                            default: HTTP
                            description: URIScheme identifies the scheme used for
                              connection to a host for Get actions
                            enum:
                            - HTTP
                            - HTTPS
                            type: string
                        required:
                        - port
                        type: object
                      initialDelaySeconds:
                        default: 0
                        format: int32
                        maximum: 300
                        minimum: 0
                        type: integer
                      periodSeconds:
                        default: 10
                        format: int32
                        maximum: 300
                        minimum: 1
                        type: integer
                      successThreshold:
                        default: 1
                        format: int32
                        maximum: 10
                        minimum: 1
                        type: integer
                      tcpSocket:
                        description: TCPSocketAction is the safe TCP probe subset
                          supported by v0.1.
                        properties:
                          port:
                            maxLength: 15
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                            type: string
                        required:
                        - port
                        type: object
                      timeoutSeconds:
                        default: 1
                        format: int32
                        maximum: 60
                        minimum: 1
                        type: integer
                    type: object
                    x-kubernetes-validations:
                    - message: exactly one of httpGet, tcpSocket or grpc must be specified
                      rule: '(has(self.httpGet) ? 1 : 0) + (has(self.tcpSocket) ?
                        1 : 0) + (has(self.grpc) ? 1 : 0) == 1'
                type: object
              replicas:
                format: int32
                maximum: 20
                minimum: 1
                type: integer
              resources:
                description: ResourceRequirements describes the compute resource requirements.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              service:
                description: ServiceSpec configures the optional ClusterIP Service.
                properties:
                  enabled:
                    type: boolean
                  ports:
                    items:
                      description: ServicePortSpec declares a ClusterIP Service port.
                      properties:
                        name:
                          maxLength: 15
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        port:
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        protocol:
                          default: TCP
                          description: Protocol defines network protocols supported
                            for things like container ports.
                          enum:
                          - TCP
                          - UDP
                          - SCTP
                          type: string
                        targetPort:
                          description: TargetPort references a declared container
                            port by name.
                          maxLength: 15
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                      required:
                      - name
                      - port
                      - targetPort
                      type: object
                    maxItems: 32
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                required:
                - enabled
                type: object
                x-kubernetes-validations:
                - message: service ports must be declared when service is enabled
                  rule: '!self.enabled || (has(self.ports) && size(self.ports) > 0)'
            required:
            - image
            - managementPolicy
            - replicas
            - resources
            type: object
            x-kubernetes-validations:
            - message: every service targetPort must reference a declared container
                port name
              rule: '!has(self.service) || !self.service.enabled || (has(self.ports)
                && self.service.ports.all(sp, self.ports.exists(cp, cp.name == sp.targetPort)))'
            - message: an enabled PDB requires replicas >= 2 and minAvailable between
                1 and replicas-1
              rule: '!has(self.availability) || !has(self.availability.pdb) || !self.availability.pdb.enabled
                || (self.replicas >= 2 && has(self.availability.pdb.minAvailable)
                && self.availability.pdb.minAvailable < self.replicas)'
            - message: cpu and memory requests and limits are required
              rule: has(self.resources.requests) && has(self.resources.limits) &&
                'cpu' in self.resources.requests && 'memory' in self.resources.requests
                && 'cpu' in self.resources.limits && 'memory' in self.resources.limits
          status:
            description: CoffeeShopServiceStatus defines the observed state reported
              in Phase 6.2.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              desiredReplicas:
                format: int32
                type: integer
              observedGeneration:
                format: int64
                type: integer
              readyReplicas:
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
