apiVersion: v1
kind: Service
metadata:
  name: platform-ownership-guard-metrics-service
  namespace: platform-ownership-guard-system
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: platform-ownership-guard
spec:
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: platform-ownership-guard
